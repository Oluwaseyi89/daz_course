import React from 'react';

import '../../styles/settings.css';

function Settings (props) {

    return (
        <div className='settings'>"I am Settings component"</div>
    );

};

export default Settings